<!-- This file is updated by both founder and agent. Do not treat as static. -->

# Office OS — Memory

## Vision
Internal leverage engine — founder execution multiplier. Not a product yet. Autonomous system that reads structured memory, executes tasks, logs decisions, and produces real artifacts without requiring constant founder reactivation. Long-term: fully autonomous office that finds leads, contacts them, follows up, and routes opportunities.

## Current Milestone
Completing Path B pivot — building memory layer, agent contract, task system, and runner to achieve first autonomous execution. Architecture defined (dispatcher, departments). Sales system partially wired. Missing: real send layer, real lead ingestion, feedback loop.

## Active Offer
None — internal infrastructure only.

## Positioning
"Founder execution multiplier." Not an AI agent system — a carry-forward execution loop that compounds.

## Backlog
- Complete memory layer (step 1 — in progress)
- Create system files (agent_prompt.txt, escalation_rules.md)
- Build task YAML system
- Create decision log scaffolding
- Build agent.py CLI wrapper
- Build verify.py post-run checks
- Set up GitHub Action for nightly runs
- Integrate send layer for sales
- Integrate lead ingestion
- Integrate reply detection

## Queue
<!-- Founder populates after reviewing backlog -->

## Blockers
- Memory files need founder review and input | severity: critical | owner: founder
- Current system scores 11/100 on autonomy — generates text but cannot send, source, or detect | severity: critical | owner: agent
- Risk of overengineering agents before proving messages land and replies happen | severity: high | owner: founder

## Context Window
Office OS is the infrastructure everything else runs on. Build order: memory layer → system files → task YAML → decision logs → agent.py → verify.py → automation. Do not skip ahead. The simple hierarchy is: Lumen = entry point (money), Merlin = platform (scale), Office OS = force multiplier (distribution), ShopTrack = optional cashflow.

## Last Updated
2026-04-17 12:00 by founder
